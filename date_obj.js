const date = Date(); // returns current date string
console.log(date, typeof date);

const date_obj = new Date(); // returns date object with the current time
console.log(date_obj, typeof date_obj);

const date_obj_0 = new Date("");
